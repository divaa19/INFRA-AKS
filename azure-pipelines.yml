# Infrastructre deployement -AKS Cluster deploy

trigger:
  - main

variables:
  - template: parameters.yml

stages:
- stage: Build 
  displayName: Build 
  jobs:
  - job: Build 
    displayName: Build 
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: AzureResourceManagerTemplateDeployment@3
      inputs:
        deploymentScope: 'Resource Group'
        azureResourceManagerConnection: ${{ variables.rmconnection }}
        subscriptionId: ${{ variables.subscription }}
        action: 'Create Or Update Resource Group'
        resourceGroupName: ${{ variables.resourceGroup }}
        location: 'Central US'
        templateLocation: 'Linked artifact'
        csmFile: '$(Pipeline.Workspace)/s/template.json'
        csmParametersFile: '$(Pipeline.Workspace)/s/parametersFile.json'
        deploymentMode: 'Incremental'
        
